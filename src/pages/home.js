import React from "react";
import { Container } from "@material-ui/core";

const Home = () => {
  return (
    <Container>
      <h1>사람멀미 처방전 - 직장인의 마음(心), 귀(耳), 입(口) 사용법</h1>
      <h2>저자: 남충희</h2>
    </Container>
  );
};

export default Home;
